# Other-agent originals

Drop pictures/video copied from another Cursor/Grok agent run here, then add rows to `../MANIFEST.json` and `VISUAL_CORPUS_CATALOG`.

Sibling agent VMs are **not** mounted in new windows. If it is not in this folder (or `current-app/` / `walkthrough/`), the team cannot see it.
